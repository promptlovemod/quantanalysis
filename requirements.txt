# Stock ML Analyzer v10.0 — Requirements
#
# STEP 1: PyTorch with CUDA 12.1 (RTX 3050)
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# STEP 2:
#   pip install -r requirements.txt
#
# STEP 3 (optional but recommended):
#   pip install catboost       # extra tree model [5/7]
#   pip install hmmlearn       # NEW 13: regime HMM (graceful fallback if missing)

# Core data
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0

# ML — core (ALL REQUIRED)
scikit-learn>=1.5.0       # 1.5+ REQUIRED (multi_class param removed in 1.5)
xgboost>=2.0.0
lightgbm>=4.0.0

# ML — optional
# catboost>=1.2.0
# hmmlearn>=0.3.0           # for NEW 13 regime-conditional model

# Charting
matplotlib>=3.7.0
seaborn>=0.12.0

# Statistics
statsmodels>=0.14.0
scipy>=1.11.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0
